{{- /* JSON feed of all non-draft articles — used by masonry loader and search */}}
{{- $all := where (where (where site.RegularPages "Params.draft" "!=" true) "Layout" "ne" "about") "Layout" "ne" "friends" }}
[
  {{- range $i, $p := $all }}
  {{- if $i }},{{ end }}
  {
    "title": {{ $p.Title | jsonify }},
    "slug": {{ $p.Slug | default ($p.Title | urlize) | jsonify }},
    "date": {{ $p.Date | time.Format "2006-01-02" | jsonify }},
    "description": {{ $p.Params.description | default $p.Summary | jsonify }},
    "content": {{ $p.Plain | jsonify }},
    "subtitle": {{ $p.Params.subtitle | jsonify }},
    "cover": {{ $p.Params.cover | jsonify }},
    "categories": {{ $p.Params.categories | default slice | jsonify }},
    "tags": {{ $p.Params.tags | default slice | jsonify }},
    "featured": {{ $p.Params.featured | default false }},
    "permalink": {{ $p.RelPermalink | jsonify }},
    "readingTime": {{ $p.ReadingTime }}
  }{{- end }}
]
